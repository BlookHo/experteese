.form_select
  = form_tag("/display_image_result", remote: true) do
    = select_tag :file_name,  options_for_select(@pictures_files, selected: @selected_file), prompt: "Select file from list"
    %button.button__choose
      = submit_tag 'select'



